# Проект трансформации ДУП — ТОО «Көркем Телеком»

Комплект документов для реализации проекта оптимизации и трансформации процессов Департамента управления проектами.

**Источник материалов:** [Google Drive — папка ДУП](https://drive.google.com/drive/folders/1fM-_x_Lva70JDIzi97QsIJPpjLpywMpD)

---

## Структура проекта

```
dup_project/
├── README.md                          ← вы здесь
├── 00_Управление_проектом/
│   ├── 01_Паспорт_проекта.md          ← цели, scope, KPI, риски
│   ├── 02_Дорожная_карта.md           ← 5 фаз, gate-критерии
│   └── 03_План_реализации.md          ← WBS, задачи, коммуникации
├── 01_Процессы/
│   ├── DUP_реестр_бизнес_процессов.xlsx ← реестр процессов + владельцы
│   ├── build_process_registry.py      ← генератор реестра
│   ├── ДУП_карта_ролей_в_процессах.xlsx ← где ДУП владелец, где участник
│   ├── build_dup_role_map.py          ← генератор карты ролей
│   ├── ДУП_роли_в_процессах_кратко.xlsx ← краткая версия: 1 вкладка, 5 колонок
│   ├── build_dup_role_map_simple.py   ← генератор краткой версии
│   ├── ДУП_метрики_процессов.xlsx     ← критерии оценки по Методике KT_METHOD
│   ├── build_dup_metrics.py           ← генератор критериев оценки
│   ├── ДУП_приоритеты_реинжиниринга.xlsx ← очередь работ и обоснование по каждому объекту
│   └── build_dup_priority.py          ← генератор приоритетов
├── 02_Презентация/
│   ├── ДУП_Трансформация_процессов_презентация.pptx ← 20 слайдов по брендбуку
│   ├── ДУП_Приоритеты_реинжиниринга_презентация.pptx ← 18 слайдов, действующий доклад
│   ├── brand.py                       ← фирменный стиль Sergek (тема SG_pptx)
│   ├── build_presentation.py          ← генератор ранней версии доклада
│   └── build_priority_deck.py         ← генератор действующего доклада
├── ВНД/
│   └── KT_METHOD_методика системы управления процессами_v1.pdf ← источник метрик
└── materials/                         ← исходные материалы с Drive
    ├── Слой А (текущий анализ)/
    ├── Слой B (наследие Confluence)/
    └── Опросник ДУП/
```

---

## Ключевые выводы анализа

1. **12 из 15** узких мест уже описаны в методологии — проблема в **исполнении**, не в проектировании.
2. **2 подлинных пробела:** gate готовности продукта (1.2) и handover реализация→сопровождение (2.0).
3. **Организационный риск:** блоки 1 и 2 сегодня ведёт один человек (Власов Д.О.).
4. **434 документа** в реестре методологии — нужна актуализация, не переписывание.

---

## С чего начать (CEO checklist)

- [ ] Утвердить паспорт проекта и назначить PM
- [ ] Созвать первое Steering Committee
- [ ] Запустить опросник филиалов (Google Form готов)
- [ ] Назначить пилотный филиал для аудита
- [ ] Инициировать интервью с ДРБ и Финблоком
- [ ] Поставить паузу на утверждение ДИ до сверки с методологией

---

## Регенерация реестра процессов

```bash
cd dup_project/01_Процессы
python3 build_process_registry.py
```

## Карта ролей ДУП в процессах

`ДУП_карта_ролей_в_процессах.xlsx` показывает по каждому процессу, подпроцессу
и этапу, где ДУП — генеральный бизнес-владелец, а где лишь участник процесса
другого подразделения.

Ключевое различие: подпроцесс в дереве ДУП и сквозной процесс — не одно и то же.
«Формирование реестра платежей» (1.9) внутри ДУП ведёт зам. по реализации, но
сквозным процессом оплаты владеет Финблок. Так же устроены ФЭМ (1.5), закупки
(1.7) и прогноз поступлений (2.3).

Владелец закреплён у каждого объекта. Пятнадцать записей, по которым владение
отсутствовало или было размыто, закрыты решениями по главе 4 Методики: владелец
определяется пятью критериями Таблицы 5, применяемыми строго последовательно,
а уровень должности — Таблицей 4. Сборка падает, если в реестре снова появится
объект без роли A.

Листы: `Легенда`, `Карта ролей ДУП` (62 записи, автофильтр),
`ДУП — генеральный владелец`, `ДУП — участник`, `Назначение владельцев`,
`Сводка`, `Выводы`, `Матрица стыков с ЦО`.

```bash
cd dup_project/01_Процессы
python3 build_dup_role_map.py
```

### Краткая версия

`ДУП_роли_в_процессах_кратко.xlsx` — одна вкладка и пять колонок: уровень,
наименование, бизнес-владелец, роль ДУП и что ДУП делает.

Роль принимает четыре значения и определяется позицией ДУП относительно
результата объекта: **владелец** (объект принадлежит ДУП), **участник** (ДУП
выполняет внутри объекта шаг, создающий часть результата), **клиент** (ДУП
стоит на выходе объекта, формулирует требования и принимает результат) и
**не определён** (владелец не закреплён — записей с этим значением сейчас нет,
оно оставлено как контрольное). Поэтому подпроцессы блока 1, которыми
на самом деле владеют Финблок или Закупщики, помечены как участие, а приёмка
пакета от ДРБ, поставки от Закупщиков и оборудования от СТ — как клиентская
позиция.

Файл строится из тех же данных, что и подробная карта — правка вносится один
раз в `build_dup_role_map.py`, после чего пересобираются оба файла.

```bash
cd dup_project/01_Процессы
python3 build_dup_role_map.py && python3 build_dup_role_map_simple.py
```

## Критерии оценки процессов

`ДУП_метрики_процессов.xlsx` задаёт, по каким показателям оценивается каждый
процесс, подпроцесс и этап. Состав метрик, формулы, типовые целевые значения,
форма паспорта процесса и модель зрелости взяты из
[Методики системы управления бизнес-процессами](ВНД/KT_METHOD_методика%20системы%20управления%20процессами_v1.pdf)
ТОО «Көркем Телеком» (KT_METHOD, v1).

Набор метрик определяется ролью ДУП в объекте: владелец отвечает за все четыре
группы метрик и ведёт паспорт процесса, участник фиксирует фактические значения
по своему шагу, клиент оценивает вход — своевременность, полноту и качество
полученного результата и CSAT internal через ГВК. Для объектов без владельца
метрики установить нельзя до применения процедуры §5 Главы 5 Методики — сейчас
таких объектов нет, поэтому все 62 объекта получили полный профиль метрик.

Листы: `Как читать`, `Критерии оценки` (423 строки, автофильтр),
`Метрики по объектам`, `Справочник метрик`, `Паспорт процесса`,
`Оценка зрелости`.

```bash
cd dup_project/01_Процессы
python3 build_dup_metrics.py
```

## Приоритеты реинжиниринга

`ДУП_приоритеты_реинжиниринга.xlsx` отвечает на вопрос «с чего начинать».
Каждый из 62 объектов реестра оценён по пяти критериям — влияние на результат
(вес 30), экономия времени (20), экономия денег (20), риск бездействия (15) и
лёгкость реализации (15) — и получил итоговый балл по шкале 0–100, место в
очереди работ и письменное обоснование, почему берётся именно там. На листе
«Как читать» описаны все пять ступеней каждой шкалы, а не только края
и середина: оценку 2 или 4 нужно уметь защитить так же, как оценку 5.

Очередь строится в два прохода. Сначала объект попадает в волну — это решение
учитывает зависимости: по п. 33 Методики целевые значения метрик можно
адресовать только существующей должности, поэтому объекты, чьё владение ещё
не доведено до приказа, идут первыми; по п. 140 узкое место берётся раньше
соседних шагов, иначе общее время не сократится. Внутри волны объекты идут
по убыванию балла, но не раньше своих предшественников — сборка проверяет, что
предшественник не стоит в более поздней волне. Сортировка листа «Приоритеты»
по колонке «Итоговый балл» даёт чистый рейтинг ценности без учёта зависимостей.

Первая волна — семь объектов: чек-лист передачи между блоками 1 и 2, приказ
о разделении заместителей, приказ о полномочиях ПМО, базовая линия по аудиту
и норматив срока кадровых приказов с HR.

Листы: `Как читать`, `Приоритеты` (62 объекта, автофильтр), `Порядок работ`
(четыре волны с целями и gate-критериями), `Первые 11 шагов`,
`Матрица ценность — лёгкость`.

```bash
cd dup_project/01_Процессы
python3 build_dup_priority.py
```

## Регенерация презентаций

Требуется корпоративный шаблон брендбука в `dup_project/brandbook/` — подробности
и порядок сборки описаны в [02_Презентация/README.md](02_Презентация/README.md).

```bash
cd dup_project/02_Презентация
python3 build_presentation.py       # ранняя версия доклада, 20 слайдов
python3 build_priority_deck.py      # действующий доклад, 18 слайдов
```

Действующий доклад ведёт от предпосылок проекта и проведённой работы через
определение системы управления процессами и формулы метрик к диагнозу AS-IS,
целевому состоянию и очереди работ. Объекты, баллы, волны, правила очерёдности,
формулы метрик и оценка зрелости берутся прямо из `build_dup_priority.py`
и `build_dup_metrics.py`, поэтому слайды не могут разойтись с Excel-файлами.

---

## Связанные материалы в репозитории

- `surveys/` — опросник, Google Forms, анализ ответов
- `materials/dup_process_tree_2.html` — дерево процессов v4
